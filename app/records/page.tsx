import Link from "next/link";import {getMealRecordsForMonth} from "@/lib/actions/meals";
export default async function RecordsPage(){const month=new Date().toISOString().slice(0,7);const records=await getMealRecordsForMonth(month);return <main className="py-7"><div className="container-app space-y-5"><div className="flex justify-between"><div><h1 className="text-2xl font-black">식사 기록</h1><p className="text-sm text-gray-500">{month} 기록</p></div><Link href="/" className="btn btn-secondary">요약으로</Link></div><div className="card overflow-x-auto"><table className="w-full text-sm"><thead className="bg-gray-50"><tr><th className="p-3 text-left">날짜</th><th className="p-3 text-left">식당</th><th className="p-3 text-left">참여자</th><th className="p-3 text-right">총액</th></tr></thead><tbody>{records.map(r=><tr key={r.id} className="border-t"><td className="p-3">{r.mealDate}</td><td className="p-3 font-semibold">{r.restaurantName??"-"}</td><td className="p-3 text-gray-600">{r.participants.map(p=>`${p.name} ${p.amount.toLocaleString()}원`).join(", ")}</td><td className="p-3 text-right font-bold">{r.totalAmount.toLocaleString()}원</td></tr>)}</tbody></table></div></div></main>}