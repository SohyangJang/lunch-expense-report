"use client";
import {useState} from "react";import {addMealRecord,type ParticipantInput} from "@/lib/actions/meals";
type Member={id:number;name:string;position:string|null};type Restaurant={id:number;name:string;menu:string|null};
export function RegisterMealForm({members,restaurants}:{members:Member[];restaurants:Restaurant[]}){const[date,setDate]=useState(new Date().toISOString().slice(0,10)),[restaurantId,setRestaurantId]=useState(""),[selected,setSelected]=useState<Record<number,string>>({}),[pending,setPending]=useState(false);
const total=Object.values(selected).reduce((s,v)=>s+(Number(v)||0),0);function toggle(id:number){setSelected(p=>{const n={...p};if(id in n)delete n[id];else n[id]="";return n;});}
async function submit(e:React.FormEvent){e.preventDefault();const participants:ParticipantInput[]=Object.entries(selected).map(([id,amount])=>({memberId:Number(id),amount:Number(amount)}));setPending(true);try{await addMealRecord(date,Number(restaurantId),participants);setSelected({});setRestaurantId("");alert("식사 기록을 저장했습니다.");}catch(e){alert(e instanceof Error?e.message:"저장에 실패했습니다.");}finally{setPending(false);}}
return <form onSubmit={submit} className="card p-4 space-y-4"><div><h2 className="text-lg font-bold">점심 식사 등록</h2><p className="text-sm text-gray-500">참여자별 부담액을 입력하면 총액이 자동 계산됩니다.</p></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-2"><input className="field" type="date" value={date} onChange={e=>setDate(e.target.value)}/><select className="field" value={restaurantId} onChange={e=>setRestaurantId(e.target.value)}><option value="">식당 선택</option>{restaurants.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></div><div className="space-y-2">{members.map(m=><div key={m.id} className="flex items-center gap-2 border rounded-xl p-2"><input type="checkbox" checked={m.id in selected} onChange={()=>toggle(m.id)}/><div className="flex-1"><b className="text-sm">{m.name}</b><div className="text-xs text-gray-500">{m.position??""}</div></div>{m.id in selected&&<input className="field w-32" type="number" min="1" step="100" placeholder="금액" value={selected[m.id]} onChange={e=>setSelected(p=>({...p,[m.id]:e.target.value}))}/>}</div>)}</div><div className="flex justify-between rounded-xl bg-gray-50 p-3"><b>총액</b><b>{total.toLocaleString()}원</b></div><button className="btn btn-primary w-full" disabled={pending}>{pending?"저장 중...":"식사 기록 저장"}</button></form>}